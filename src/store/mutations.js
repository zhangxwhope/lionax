export default {
  // 更新详情页导航数据
  updateDetailNav (state, nav) {
    state.detailNav = nav
  },
  // 更新baseUrl
  updateBaseUrl (state, url) {
    state.baseUrl = url
  },
  // 更新免责声明同意标志
  updateProtocalAgree (state, agree) {
    state.protocalAgree = agree
  }
}
